const update = (x)=>{
    updatedElement = [x.model]
    btn.value = "Update"
    brand.value = x.brand;
    model.value = x.model;
    price.value = x.price;
    tax.value = x.tax;
    category.value = x.category;
    discount.value = x.discount;
    total.value = x.total;
    count.value = x.count;
}